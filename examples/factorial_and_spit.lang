# Example showing factorial operator and spit() function

# Calculate and print factorial of 5
result = 5!;
spit(result);

# Calculate factorial of a variable
n = 4;
fact_n = n!;
spit( n, fact_n);

# Calculate factorial in an expression
spit( 3! + 4);

# Multiple arguments to spit
spit(42, 7!);